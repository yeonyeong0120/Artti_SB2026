namespace Artti.SignBridge.UI
{
    /// <summary>앱 화면 식별자(plan §7 화면 구성).</summary>
    public enum AppScreen
    {
        Splash,
        CameraPermission,
        Home,
        Recognition,
        SessionLog,
        AllLogs,
        Settings,
    }
}
